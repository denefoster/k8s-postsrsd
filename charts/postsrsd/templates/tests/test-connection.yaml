apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postsrsd.fullname" . }}-test-connection"
  labels:
    {{- include "postsrsd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postsrsd.fullname" . }}:{{ .Values.service.socket.port }}']
  restartPolicy: Never
